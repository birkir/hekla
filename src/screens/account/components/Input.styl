.host {
  &__ios {
    border: 1px solid;
    border-top-color: var(--content-border);
    border-bottom-color: var(--content-border);
    border-left-color: var(--content-border);
    border-right-color: var(--content-border);
    border-radius: 4px;
  }

  &__android {
    margin-top: -12px;
  }
}

.input {
  font-size: 15px;
  padding: 12px;
  color: var(--content-fg);

  &__android {
    padding: 16px 0px;
  }
}
