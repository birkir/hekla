.host {
  flex: 1;
  background-color: var(--content-bg-color);
}

.container {
  margin-top: 30px;

  align-items: center;
  justify-content: center;
}

.login {
  flex: 1;
  max-width: 350px;

  align-items: center;
  justify-content: center;
}

.logo {
  tint-color: #ff6600;

  margin-bottom 16px;
}

.text {
  font-family: 'System';
  font-size: 15px;
  text-align: center;
  color: var(--content-text-color);

  margin-bottom: 16px;
  padding: 0 30px;
}

.button {
  width: 100%;
  padding: 0 30px;
  margin-bottom 8px;
}
