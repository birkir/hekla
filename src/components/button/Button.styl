.button {
  &__disabled {
    elevation: 0px;
    background-color: #DFDFDF;
  }

  &__fill {
    background-color: #007AFF;
    border-radius: 4px;
  }
}

.text {
  font-size: 18px;
  color: #007AFF;
  text-align: center;
  padding: 12px;

  &__fill {
    color: #FFFFFF;
  }

  &__disabled {
    color: #CDCDCD;
  }
}

.loading {
  padding: 12px;
}
